/*
 *
 * (Avatar, name) -> Avatar
 *
 * given an Avatar object and a new name, returns an Avatar object with an updated name value
 *
 */
export const updateNameHandler = (avatar, name) => {
  return {
    id: 1,
    name: 'george',
    health: 8,
  };
};
